dependencies:
  override:
    - npm install -g eslint
    - npm install
    - docker version
machine:
  services:
    - docker
  node:
    version: 5.4
test:
  override:
    - make
deployment:
  hub:
    branch: master
    owner: opsee
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS quay.io
      - make docker-push
